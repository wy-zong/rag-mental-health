# 本次修訂（revision/）所需套件。
# 原始專案的 requirements.txt 保持不動，以免影響既有腳本的重現性。

# --- 資料處理 ---
pandas>=2.0
numpy>=1.24

# --- 檢索 ---
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0     # 會一併帶入 torch

# --- 統計（McNemar / bootstrap / Wilson CI）---
scipy>=1.10
scikit-learn>=1.3

# --- Prompt 搜尋（--search tpe 模式才需要）---
optuna>=3.0

# --- LLM 推論 ---
ollama>=0.3.0

# --- Baselines（07_baselines.py）---
transformers>=4.35

# --- 表格輸出（DataFrame.to_markdown）---
tabulate>=0.9

# --- 繪圖 ---
matplotlib>=3.7

# --- 環境探測 ---
psutil>=5.9
tqdm>=4.65
